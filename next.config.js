/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 如果你需要从外部域加载图片，可以在这里配置
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig; 