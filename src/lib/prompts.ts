import { LLMStage1Scores, AnalyzeDataPayload } from "@/types";\n\n/**\n * 生成用于第一阶段多模态LLM分析的Prompt。\n * @param payload - 包含面部、声音、心率等数据的对象。\n * @param facial_image_description - (可选) 如果LLM不直接处理图像，而是描述性文本。\n * @param voice_transcription - (可选) 声音转录文本。\n */\nexport function getMultiModalAnalysisPrompt(\n  payload: AnalyzeDataPayload,\n  facial_image_description?: string, \n  voice_transcription?: string\n): string {\n  let prompt = `你是一位专业的健康顾问和临床心理分析师，擅长通过多模态信息综合评估个体的生理和心理状态。\n请根据以下提供的多模态数据，进行综合分析。\n\n[任务说明]\n1. 仔细分析提供的所有数据维度。\n2. 重点评估情绪状态、压力水平、以及整体的生理与心理健康趋势。\n3. 你的输出必须是严格的JSON格式，包含以下字段，评分范围为1-10分 (1分表示极差/极度负面，10分表示极好/极度正面)。\n   每个评估都应包含一个 \"score\" (数值型) 和一个 \"details\" (简要说明，解释评分依据，100字以内)。\n   - \"facial_expression_assessment\": { \"score\": number, \"details\": string } (评估面部表情所反映的情绪，如快乐、悲伤、焦虑、平静等及其强度)\n   - \"vocal_characteristics_assessment\": { \"score\": number, \"details\": string } (评估声音特征，如语调、语速、音量、流畅度、情感色彩，判断其反映的能量水平和情绪状态)\n   - \"physiological_state_assessment\": { \"score\": number, \"details\": string } (基于心率数据推断生理激活水平，如平静、放松、紧张、激动等)\n   - \"estimated_stress_level\": { \"score\": number, \"category\": string } (综合评估当前压力水平，\"category\"可以是：低、中、高)\n   - \"overall_wellbeing_score\": { \"score\": number, \"trend_suggestion\": string } (综合以上所有信息的整体幸福感/健康状态评分，\"trend_suggestion\" 简述当前状态的积极或消极趋势)\n\n[输入数据]\n`;\n\n  if (payload.facialData) {\n    if (facial_image_description) {\n      prompt += `- 面部信息 (描述): ${facial_image_description}\\n`;\n    }\n    else {\n      // 假设LLM可以直接处理图像数据，或者这里应有提示指导LLM如何获取图像\n      prompt += `- 面部信息: [包含一张需要分析的面部图像，请识别主要情绪及其置信度。]\\n`; \n    }\n  }\n  if (payload.voiceData) {\n    if (voice_transcription) {\n      prompt += `- 声音信息 (转录): \"${voice_transcription}\"\\n`;\n    }\n    // 假设LLM可以直接处理音频数据，或者这里应有提示指导LLM如何获取音频\n    prompt += `- 声音信息: [包含一段需要分析的音频，请分析其韵律、情感倾向。]\\n`; \n  }\n  if (payload.heartRate !== undefined) {\n    prompt += `- 生理数据: 当前心率 ${payload.heartRate} bpm。\\n`;\n  }\n  if (!payload.facialData && !payload.voiceData && payload.heartRate === undefined) {\n    prompt += `- 无有效数据输入，请告知用户需要提供数据。\\n`;\n  }\n\n  if (payload.previousSummary) {\n    prompt += `\\n[历史参考]\n- 上次分析总结: \"${payload.previousSummary}\"\\n  请在本次分析中考虑此历史信息，并关注可能的变化。\\n`;\n  }\n\n  prompt += `\\n[输出要求]\n请严格按照上述JSON格式提供评分和详细说明。确保所有评分都在1-10的范围内。`;\n\n  return prompt;\n}\n\n/**\n * 生成用于第二阶段LLM建议的Prompt。\n * @param scores - 第一阶段LLM返回的打分对象。\n * @param previous_summary - (可选) 用户之前的健康总结。\n */\nexport function getAdvicePrompt(scores: LLMStage1Scores, previous_summary?: string): string {\n  let prompt = `你是一位经验丰富的健康管理专家、心理咨询师和生活教练。\n\n[任务说明]\n基于以下提供的健康评估分数，并可选参考用户过往的健康总结，请提供一份详细的、个性化的健康分析报告和行动建议。\n您的报告应包含以下部分，并以清晰的Markdown格式呈现：\n\n1.  **当前状态概览 (Current Status Overview)**:\n    *   简洁概括用户当前的主要生理和心理状态，点出最显著的积极方面和潜在的风险点 (约150-200字)。\n\n2.  **各项指标深度解读 (Detailed Score Analysis)**:\n    *   针对每一项评分进行详细解释，说明该分数可能意味着什么，结合具体数据（如心率数值，表情和声音中可能的情感信号）进行阐述。\n    *   如果某些数据缺失，也请指出。\n\n3.  **个性化行动建议 (Personalized Actionable Advice)**:\n    *   提供至少3-5条具体的、可操作的建议。建议应针对性强，能够帮助用户改善评分较低的方面或保持积极状态。\n    *   建议可以涵盖：\n        *   生活方式调整 (如作息、饮食、运动建议)\n        *   情绪管理技巧 (如正念练习、应对技巧)\n        *   压力应对策略 (如放松技巧、时间管理)\n        *   社交互动建议 (如果适用)\n    *   确保建议具有科学依据，易于理解和实践。\n\n4.  **后续关注与提升方向 (Focus Areas for Future Improvement)**:\n    *   指出用户未来可以持续关注和努力提升的关键领域。\n    *   鼓励用户积极面对并采取行动。\n\n[输入数据]\n健康评估分数:\n\`\`\`json\n${JSON.stringify(scores, null, 2)}\n\`\`\`\n`;\n\n  if (previous_summary) {\n    prompt += `\\n用户历史健康总结 (供参考，请分析本次与历史的变化趋势):\n\"${previous_summary}\"\\n`;\n  }\n\n  prompt += `\\n[输出要求]\n*   请使用Markdown格式化您的回答，确保结构清晰、易于阅读。\n*   语言应专业、富有同情心且积极向上，鼓励用户。\n*   最终输出一份完整的分析和建议报告。`;\n\n  return prompt;\n}\n 